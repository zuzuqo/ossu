The standard implementation of sorting in Python (`l.sort()`, `sorted(l)`) runs in roughly `O (n * log(n))` time, where n is the length of the list.
You will rarely need to implement your own sort function.
